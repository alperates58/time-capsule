generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Phase 1: Minimal initialization.
// Database schema will be designed and implemented in later phases.

model Placeholder {
  id Int @id @default(autoincrement())
}
